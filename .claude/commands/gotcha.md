---
description: Append a Python gotcha to python-gotchas.md
argument-hint: <description of the gotcha>
allowed-tools: Read, Edit
---
Constraint (CLAUDE.md): Python LANGUAGE errors (syntax, scoping, shadowed
builtins, off-by-one, etc.) may be explained directly and fully — those are not
the exercise, so capturing them here is encouraged.

Gotcha to record: `$ARGUMENTS`

Steps:
1. Read `python-gotchas.md` to match its existing style and avoid duplicates.
2. Append a well-written entry: a short bold title, one or two sentences on what
   goes wrong and why, and a tiny illustrative snippet (wrong vs right) when it
   helps. This is about the Python language, not any problem's algorithm.
3. Keep entries skimmable — this file is a quick reference the human rereads.
