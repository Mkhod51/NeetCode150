"""
Problem: {NUMBER} {TITLE}
Link: {LEETCODE_URL}
Category: {CATEGORY}
Difficulty: {DIFFICULTY}
Pattern: TODO
Key insight: TODO
Where I got stuck: TODO
Hints used: TODO        (must end up as an integer 0-3)
Time complexity: TODO
Space complexity: TODO
Date solved: TODO       (must end up as YYYY-MM-DD)
Revisit: TODO           (must end up as yes or no)
"""
import pathlib
import sys

# Bootstrap: put the repo root on sys.path so `helpers` imports resolve. Run
# solution files from the repo root, e.g.  python arrays-hashing/0001-two-sum.py
sys.path.insert(0, str(pathlib.Path(__file__).resolve().parents[1]))

# Uncomment for linked-list / tree problems (utilities live in helpers/structures.py):
# from helpers.structures import ListNode, TreeNode, to_linked, from_linked, to_tree, from_tree


class Solution:
    pass  # your code here — write the method signature from LeetCode yourself


if __name__ == "__main__":
    # Tests go here. Write plain asserts that call your Solution and check the
    # result, then run this file from the repo root:
    #     python <category>/<file>.py
    # Exit code 0 (all asserts pass) is what marks the problem solved.
    #
    # Example shape — commented out, references no real problem's logic. Fill in
    # your own method name and cases:
    #     sol = Solution()
    #     assert sol.your_method(example_input) == expected_output
    pass
